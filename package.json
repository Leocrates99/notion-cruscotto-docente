{
  "name": "notion-cruscotto-docente",
  "version": "1.0.0",
  "description": "Costruzione via codice del cruscotto del docente in Notion: schema, relazioni, rollup, formule e dati di esempio. Notion API SDK v5, modello data source 2025-09-03.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsx src/build.ts",
    "seed": "tsx src/seedRun.ts",
    "verify": "tsx src/verify.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@notionhq/client": "^5.0.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
